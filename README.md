# 🔍 Scout Bot - Telegram бот-разведчик

Бот для мониторинга сообщений в Telegram чатах по ключевым словам.

## 🚀 Возможности

- ✅ Мониторинг групповых чатов из выбранной папки
- ✅ Поиск по ключевым словам и фразам
- ✅ Уведомления в личку через бота
- ✅ Веб-интерфейс для настройки
- ✅ Поддержка двухфакторной аутентификации
- ✅ Безопасное хранение сессий

## ⚠️ Ограничения (защита от бана)

- Максимум 50 чатов в мониторинге
- Только групповые чаты (не личные)
- Только новые сообщения (не история)
- Бот не отправляет сообщения в чаты
- Соблюдение rate limits Telegram

## 📋 Требования

- Node.js 18+
- npm или yarn

## 🛠 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/your-username/telegram-scout-bot.git
cd telegram-scout-bot
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Настройте переменные окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Токен вашего бота (получите у @BotFather)
BOT_TOKEN=your_bot_token_here

# Порт сервера
PORT=3000

# Секретный ключ для сессий
SESSION_SECRET=your-super-secret-key

# URL приложения (для локальной разработки)
BASE_URL=http://localhost:3000
```

### 4. Запустите

```bash
npm start
```

Или в режиме разработки:

```bash
npm run dev
```

## 🌐 Деплой на Railway

### 1. Создайте новый проект на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Создайте новый проект → Deploy from GitHub
3. Выберите репозиторий

### 2. Добавьте переменные окружения

В настройках проекта добавьте:

```
BOT_TOKEN=your_bot_token
PORT=3000
SESSION_SECRET=your-very-long-secret-key-for-production
BASE_URL=https://your-app.railway.app
NODE_ENV=production
```

### 3. Деплой

Railway автоматически задеплоит приложение при каждом push в репозиторий.

## 📱 Использование

### Для пользователей

1. Откройте веб-интерфейс: `http://localhost:3000` (или ваш домен на Railway)
2. Введите API ID и Hash с [my.telegram.org](https://my.telegram.org)
3. Введите номер телефона
4. Подтвердите код из Telegram
5. Если включена 2FA - введите пароль
6. Выберите папку с чатами для мониторинга
7. Введите ключевые слова
8. Нажмите "Запустить мониторинг"
9. Откройте бота и нажмите Start

### Команды бота

- `/start` - начать работу
- `/status` - статус мониторинга
- `/stop` - остановить мониторинг
- `/help` - справка

## 🔧 API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/auth/send-code` | Отправить код подтверждения |
| POST | `/api/auth/verify-code` | Подтвердить код |
| POST | `/api/auth/verify-2fa` | Подтвердить пароль 2FA |
| GET | `/api/folders` | Получить список папок |
| GET | `/api/folders/:name/chats` | Получить чаты из папки |
| POST | `/api/monitoring/start` | Запустить мониторинг |
| POST | `/api/monitoring/stop` | Остановить мониторинг |
| GET | `/api/monitoring/status/:userId` | Статус мониторинга |
| GET | `/health` | Health check |

## 📁 Структура проекта

```
├── server/
│   ├── index.js          # Express сервер и API
│   ├── bot.js            # Telegram бот для уведомлений
│   ├── monitor.js        # MTProto мониторинг
│   ├── database.js       # SQLite база данных
│   └── keywords.js       # Логика поиска по ключевым словам
├── client/
│   └── index.html        # Веб-интерфейс
├── data/                  # Папка с базой данных (создается автоматически)
├── .env.example
├── .gitignore
├── package.json
└── README.md
```

## 🔒 Безопасность

- Сессии Telegram хранятся в зашифрованном виде в SQLite
- API credentials не передаются третьим сторонам
- Бот не имеет доступа к личным сообщениям
- Временные сессии авторизации автоматически удаляются через 30 минут

## 🐛 Возможные проблемы

### "FLOOD_WAIT"
Telegram ограничивает количество запросов. Подождите указанное время.

### "AUTH_KEY_UNREGISTERED"
Сессия стала недействительной. Пройдите авторизацию заново.

### "SESSION_PASSWORD_NEEDED"
У вас включена двухфакторная аутентификация. Введите пароль.

## 📄 Лицензия

MIT

## 🤝 Contributing

Pull requests приветствуются!
